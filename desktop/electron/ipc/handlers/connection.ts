import { ipcMain } from 'electron';

export function registerConnectionHandlers() {
  // Handlers are registered in main.ts directly since spawn logic lives there.
  // This file kept for backward compatibility.
}
